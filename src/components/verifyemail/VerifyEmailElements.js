import styled from 'styled-components';
import { OtpPage, OtpForm, Resend } from './../otp/OtpElements';

export const VerifyEmailPage = styled(OtpPage)`

`
export const VerifyEmailForm = styled(OtpForm)` 

`
export const ResendEmail = styled(Resend)` 

`